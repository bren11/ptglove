<resources>

    <!-- Base application theme. -->
    <style name="AppTheme"  parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorMBlue</item>
        <item name="colorPrimaryDark">@color/colorMBlue</item>
        <item name="colorAccent">@color/colorMYellow</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

</resources>
